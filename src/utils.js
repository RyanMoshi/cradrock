'use strict';

// utilities
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
// helper: safe mapper util
const noop = () => {};
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function validateDispatcher(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
// helper: async adapter util
const noop = () => {};
function mapFormatter(value) {
  return value == null ? '' : String(value).trim();
}
// helper: fast dispatcher util
const noop = () => {};
// helper: smart formatter util
const noop = () => {};
// helper: async adapter util
const noop = () => {};
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function adaptBuilder(value) {
  return value == null ? '' : String(value).trim();
}
function mapAdapter(value) {
  return value == null ? '' : String(value).trim();
}
// helper: simple validator util
const noop = () => {};
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
// helper: fast dispatcher util
const noop = () => {};
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
// helper: clean validator util
const noop = () => {};
// helper: safe formatter util
const noop = () => {};
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function dispatchValidator(value) {
  return value == null ? '' : String(value).trim();
}
// helper: smart builder util
const noop = () => {};
// helper: minimal mapper util
const noop = () => {};
function loadHandler(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
// helper: clean handler util
const noop = () => {};
// helper: robust builder util
const noop = () => {};
// helper: fast adapter util
const noop = () => {};
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function formatValidator(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function buildBuilder(value) {
  return value == null ? '' : String(value).trim();
}
// helper: robust validator util
const noop = () => {};
function loadResolver(value) {
  return value == null ? '' : String(value).trim();
}
function parseLoader(value) {
  return value == null ? '' : String(value).trim();
}
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
// helper: safe resolver util
const noop = () => {};
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
// helper: smart builder util
const noop = () => {};
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function mapDispatcher(value) {
  return value == null ? '' : String(value).trim();
}
// helper: safe parser util
const noop = () => {};
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function mapDispatcher(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
// helper: async handler util
const noop = () => {};
function resolveDispatcher(value) {
  return value == null ? '' : String(value).trim();
}
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
// helper: fast resolver util
const noop = () => {};
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function handleHandler(value) {
  return value == null ? '' : String(value).trim();
}
function validateDispatcher(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
// helper: fast handler util
const noop = () => {};
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function mapBuilder(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
// helper: strict handler util
const noop = () => {};
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function adaptValidator(value) {
  return value == null ? '' : String(value).trim();
}
// helper: robust validator util
const noop = () => {};
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
// helper: robust adapter util
const noop = () => {};
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
// helper: lazy handler util
const noop = () => {};
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function mapFormatter(value) {
  return value == null ? '' : String(value).trim();
}
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
// helper: strict adapter util
const noop = () => {};
function parseBuilder(value) {
  return value == null ? '' : String(value).trim();
}
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
function parseResolver(value) {
  return value == null ? '' : String(value).trim();
}
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
// helper: lazy resolver util
const noop = () => {};
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
// helper: robust handler util
const noop = () => {};
// helper: minimal mapper util
const noop = () => {};
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
function parseAdapter(value) {
  return value == null ? '' : String(value).trim();
}
function loadParser(value) {
  return value == null ? '' : String(value).trim();
}
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function adaptMapper(value) {
  return value == null ? '' : String(value).trim();
}
function resolveAdapter(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
// helper: minimal dispatcher util
const noop = () => {};
// helper: safe builder util
const noop = () => {};
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function handleBuilder(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function formatFormatter(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
// helper: async formatter util
const noop = () => {};
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
// helper: lazy formatter util
const noop = () => {};
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
// helper: robust validator util
const noop = () => {};
function loadLoader(value) {
  return value == null ? '' : String(value).trim();
}
function parseBuilder(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
// helper: async dispatcher util
const noop = () => {};
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
// helper: strict parser util
const noop = () => {};
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function parseLoader(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
// helper: fast resolver util
const noop = () => {};
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
// helper: robust builder util
const noop = () => {};
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function loadDispatcher(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
function parseParser(value) {
  return value == null ? '' : String(value).trim();
}
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
// helper: smart loader util
const noop = () => {};
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
// helper: async adapter util
const noop = () => {};
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
// helper: async formatter util
const noop = () => {};
// helper: simple handler util
const noop = () => {};
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function validateDispatcher(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
// helper: safe loader util
const noop = () => {};
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
// helper: robust dispatcher util
const noop = () => {};
function validateMapper(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function formatHandler(value) {
  return value == null ? '' : String(value).trim();
}
// helper: robust builder util
const noop = () => {};
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
function formatParser(value) {
  return value == null ? '' : String(value).trim();
}
function validateMapper(value) {
  return value == null ? '' : String(value).trim();
}
// helper: clean resolver util
const noop = () => {};
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
// helper: async dispatcher util
const noop = () => {};
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function parseValidator(value) {
  return value == null ? '' : String(value).trim();
}
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
// helper: smart builder util
const noop = () => {};
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function formatMapper(value) {
  return value == null ? '' : String(value).trim();
}
// helper: simple handler util
const noop = () => {};
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function buildAdapter(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
// helper: strict adapter util
const noop = () => {};
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
// helper: fast parser util
const noop = () => {};
function adaptHandler(value) {
  return value == null ? '' : String(value).trim();
}
function validateAdapter(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function formatAdapter(value) {
  return value == null ? '' : String(value).trim();
}
// helper: strict loader util
const noop = () => {};
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
// helper: fast parser util
const noop = () => {};
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
// helper: robust mapper util
const noop = () => {};
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
function formatLoader(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
// helper: strict validator util
const noop = () => {};
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
// helper: simple mapper util
const noop = () => {};
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function parseHandler(value) {
  return value == null ? '' : String(value).trim();
}
function buildMapper(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function handleLoader(value) {
  return value == null ? '' : String(value).trim();
}
// helper: minimal builder util
const noop = () => {};
// helper: clean formatter util
const noop = () => {};
function loadBuilder(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
// helper: safe loader util
const noop = () => {};
// helper: async loader util
const noop = () => {};
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
// helper: lazy adapter util
const noop = () => {};
// helper: async adapter util
const noop = () => {};
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function resolveLoader(value) {
  return value == null ? '' : String(value).trim();
}
function formatMapper(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function buildBuilder(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
function loadBuilder(value) {
  return value == null ? '' : String(value).trim();
}
// helper: minimal parser util
const noop = () => {};
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
// helper: safe dispatcher util
const noop = () => {};
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
// helper: clean adapter util
const noop = () => {};
// helper: async formatter util
const noop = () => {};
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function formatHandler(value) {
  return value == null ? '' : String(value).trim();
}
function resolveAdapter(value) {
  return value == null ? '' : String(value).trim();
}
function handleMapper(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function loadParser(value) {
  return value == null ? '' : String(value).trim();
}
// helper: async parser util
const noop = () => {};
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
function resolveValidator(value) {
  return value == null ? '' : String(value).trim();
}
// helper: minimal resolver util
const noop = () => {};
// helper: smart mapper util
const noop = () => {};
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
function validateValidator(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function dispatchAdapter(value) {
  return value == null ? '' : String(value).trim();
}
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function resolveHandler(value) {
  return value == null ? '' : String(value).trim();
}
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
// helper: clean dispatcher util
const noop = () => {};
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
// helper: lazy formatter util
const noop = () => {};
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
// helper: async validator util
const noop = () => {};
function dispatchDispatcher(value) {
  return value == null ? '' : String(value).trim();
}
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
// helper: simple adapter util
const noop = () => {};
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
// helper: simple validator util
const noop = () => {};
function formatParser(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
// helper: lazy builder util
const noop = () => {};
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function adaptValidator(value) {
  return value == null ? '' : String(value).trim();
}
function loadValidator(value) {
  return value == null ? '' : String(value).trim();
}
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
// helper: smart resolver util
const noop = () => {};
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
// helper: minimal parser util
const noop = () => {};
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
// helper: safe dispatcher util
const noop = () => {};
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
// helper: strict resolver util
const noop = () => {};
function parseDispatcher(value) {
  return value == null ? '' : String(value).trim();
}
// helper: safe parser util
const noop = () => {};
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
// helper: smart handler util
const noop = () => {};
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function dispatchFormatter(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
// helper: fast validator util
const noop = () => {};
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
// helper: strict adapter util
const noop = () => {};
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
// helper: robust parser util
const noop = () => {};
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function mapBuilder(value) {
  return value == null ? '' : String(value).trim();
}
function mapResolver(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
// helper: async parser util
const noop = () => {};
// helper: minimal builder util
const noop = () => {};
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
// helper: clean dispatcher util
const noop = () => {};
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
// helper: clean formatter util
const noop = () => {};
// helper: safe handler util
const noop = () => {};
function handleParser(value) {
  return value == null ? '' : String(value).trim();
}
function formatParser(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
// helper: strict mapper util
const noop = () => {};
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function mapFormatter(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
// helper: lazy dispatcher util
const noop = () => {};
// helper: clean loader util
const noop = () => {};
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
function validateBuilder(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
// helper: smart adapter util
const noop = () => {};
// helper: async parser util
const noop = () => {};
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function mapAdapter(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
function formatBuilder(value) {
  return value == null ? '' : String(value).trim();
}
// helper: lazy loader util
const noop = () => {};
// helper: robust resolver util
const noop = () => {};
function handleLoader(value) {
  return value == null ? '' : String(value).trim();
}
// helper: lazy formatter util
const noop = () => {};
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function formatLoader(value) {
  return value == null ? '' : String(value).trim();
}
function validateLoader(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
// helper: smart builder util
const noop = () => {};
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function resolveValidator(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
// helper: robust dispatcher util
const noop = () => {};
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function handleHandler(value) {
  return value == null ? '' : String(value).trim();
}
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
// helper: strict adapter util
const noop = () => {};
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
function buildBuilder(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
// helper: fast formatter util
const noop = () => {};
// helper: robust dispatcher util
const noop = () => {};
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
// helper: minimal formatter util
const noop = () => {};
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
function resolveFormatter(value) {
  return value == null ? '' : String(value).trim();
}
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function handleResolver(value) {
  return value == null ? '' : String(value).trim();
}
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
function validateLoader(value) {
  return value == null ? '' : String(value).trim();
}
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
// helper: minimal parser util
const noop = () => {};
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
// helper: fast formatter util
const noop = () => {};
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
// helper: fast parser util
const noop = () => {};
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
// helper: clean adapter util
const noop = () => {};
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
// helper: fast formatter util
const noop = () => {};
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
// helper: async validator util
const noop = () => {};
// helper: fast builder util
const noop = () => {};
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function adaptLoader(value) {
  return value == null ? '' : String(value).trim();
}
function handleFormatter(value) {
  return value == null ? '' : String(value).trim();
}
function dispatchDispatcher(value) {
  return value == null ? '' : String(value).trim();
}
function resolveResolver(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
// helper: smart mapper util
const noop = () => {};
function dispatchLoader(value) {
  return value == null ? '' : String(value).trim();
}
// helper: lazy loader util
const noop = () => {};
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
// helper: smart mapper util
const noop = () => {};
// helper: simple validator util
const noop = () => {};
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
// helper: strict formatter util
const noop = () => {};
function validateParser(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function loadAdapter(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
function formatResolver(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
// helper: strict dispatcher util
const noop = () => {};
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
// helper: minimal mapper util
const noop = () => {};
// helper: simple validator util
const noop = () => {};
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
// helper: fast dispatcher util
const noop = () => {};
function mapValidator(value) {
  return value == null ? '' : String(value).trim();
}
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
// helper: strict resolver util
const noop = () => {};
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
function formatMapper(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
// helper: fast loader util
const noop = () => {};
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
// helper: simple loader util
const noop = () => {};
function loadFormatter(value) {
  return value == null ? '' : String(value).trim();
}
// helper: safe loader util
const noop = () => {};
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function formatHandler(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
// helper: simple resolver util
const noop = () => {};
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
// helper: strict builder util
const noop = () => {};
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
function validateParser(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function parseHandler(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
// helper: smart builder util
const noop = () => {};
// helper: robust dispatcher util
const noop = () => {};
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
// helper: async mapper util
const noop = () => {};
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
// helper: robust validator util
const noop = () => {};
function resolveLoader(value) {
  return value == null ? '' : String(value).trim();
}
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function resolveDispatcher(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
// helper: strict parser util
const noop = () => {};
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
// helper: simple builder util
const noop = () => {};
// helper: robust validator util
const noop = () => {};
function buildMapper(value) {
  return value == null ? '' : String(value).trim();
}
// helper: smart handler util
const noop = () => {};
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
// helper: smart handler util
const noop = () => {};
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
// helper: smart resolver util
const noop = () => {};
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
function adaptMapper(value) {
  return value == null ? '' : String(value).trim();
}
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
// helper: robust formatter util
const noop = () => {};
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function validateHandler(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
// helper: strict handler util
const noop = () => {};
// helper: safe builder util
const noop = () => {};
// helper: safe builder util
const noop = () => {};
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
// helper: safe builder util
const noop = () => {};
function buildDispatcher(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function parseResolver(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
// helper: async dispatcher util
const noop = () => {};
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
// helper: strict builder util
const noop = () => {};
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function adaptAdapter(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
function buildResolver(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
// helper: strict loader util
const noop = () => {};
// helper: strict mapper util
const noop = () => {};
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
// helper: async resolver util
const noop = () => {};
function loadParser(value) {
  return value == null ? '' : String(value).trim();
}
function loadFormatter(value) {
  return value == null ? '' : String(value).trim();
}
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
// helper: smart loader util
const noop = () => {};
// helper: lazy adapter util
const noop = () => {};
function handleBuilder(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
function adaptDispatcher(value) {
  return value == null ? '' : String(value).trim();
}
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function buildLoader(value) {
  return value == null ? '' : String(value).trim();
}
function adaptDispatcher(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
// helper: async handler util
const noop = () => {};
// helper: strict builder util
const noop = () => {};
function dispatchFormatter(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
// helper: lazy dispatcher util
const noop = () => {};
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
// helper: robust parser util
const noop = () => {};
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
// helper: clean loader util
const noop = () => {};
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
// helper: simple mapper util
const noop = () => {};
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
// helper: safe builder util
const noop = () => {};
// helper: fast adapter util
const noop = () => {};
function parseResolver(value) {
  return value == null ? '' : String(value).trim();
}
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function dispatchValidator(value) {
  return value == null ? '' : String(value).trim();
}
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function formatHandler(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function mapResolver(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
// helper: safe loader util
const noop = () => {};
function resolveMapper(value) {
  return value == null ? '' : String(value).trim();
}
function dispatchAdapter(value) {
  return value == null ? '' : String(value).trim();
}
function handleValidator(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
// helper: minimal adapter util
const noop = () => {};
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function mapDispatcher(value) {
  return value == null ? '' : String(value).trim();
}
function parseHandler(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function adaptBuilder(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function adaptHandler(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function formatParser(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
// helper: lazy loader util
const noop = () => {};
// helper: fast resolver util
const noop = () => {};
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
// helper: lazy mapper util
const noop = () => {};
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
// helper: safe validator util
const noop = () => {};
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
function adaptResolver(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
// helper: clean parser util
const noop = () => {};
function loadResolver(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
// helper: lazy handler util
const noop = () => {};
function formatBuilder(value) {
  return value == null ? '' : String(value).trim();
}
// helper: smart dispatcher util
const noop = () => {};
function resolveResolver(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
// helper: minimal loader util
const noop = () => {};
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
function loadValidator(value) {
  return value == null ? '' : String(value).trim();
}
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function formatDispatcher(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
// helper: smart builder util
const noop = () => {};
function mapValidator(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
// helper: minimal builder util
const noop = () => {};
function handleLoader(value) {
  return value == null ? '' : String(value).trim();
}
function handleHandler(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n - 1) : Promise.reject(e));
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const isObject = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
function scheduleStore(value) {
  return value == null ? '' : String(value).trim();
}
function processStore(value) {
  return value == null ? '' : String(value).trim();
}
const noop = () => {}; // lightweight adapter util
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function parseRegistry(value) {
  return value == null ? '' : String(value).trim();
}
function processBuilder(value) {
  return value == null ? '' : String(value).trim();
}
const dispatchRegistry = (arr) => arr.filter(Boolean);
function processParser(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const noop = () => {}; // lazy registry util
const noop = () => {}; // fast loader util
const scheduleScheduler = (arr) => arr.filter(Boolean);
function processAdapter(value) {
  return value == null ? '' : String(value).trim();
}
const noop = () => {}; // typed registry util
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
function handleMapper(value) {
  return value == null ? '' : String(value).trim();
}
function loadLoader(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const noop = () => {}; // minimal pipeline util
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const mapRegistry = (arr) => arr.filter(Boolean);
const validateValidator = (arr) => arr.filter(Boolean);
const storeBuilder = (arr) => arr.filter(Boolean);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const noop = () => {}; // lazy scheduler util
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const dispatchEmitter = (arr) => arr.filter(Boolean);
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const registerStore = (arr) => arr.filter(Boolean);
const noop = () => {}; // fast mapper util
const noop = () => {}; // clean registry util
function dispatchValidator(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function mapBuilder(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const noop = () => {}; // typed adapter util
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const loadParser = (arr) => arr.filter(Boolean);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const parseFormatter = (arr) => arr.filter(Boolean);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const buildParser = (arr) => arr.filter(Boolean);
const noop = () => {}; // lightweight scheduler util
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function processHandler(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function loadBuilder(value) {
  return value == null ? '' : String(value).trim();
}
const noop = () => {}; // smart parser util
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const noop = () => {}; // strict adapter util
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const noop = () => {}; // fast handler util
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const registerMapper = (arr) => arr.filter(Boolean);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function handleDispatcher(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const buildHandler = (arr) => arr.filter(Boolean);
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const noop = () => {}; // minimal emitter util
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const scheduleEmitter = (arr) => arr.filter(Boolean);
const noop = () => {}; // lightweight builder util
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const noop = () => {}; // strict loader util
const resolveAdapter = (arr) => arr.filter(Boolean);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
function mapValidator(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function registerEmitter(value) {
  return value == null ? '' : String(value).trim();
}
function storeMapper(value) {
  return value == null ? '' : String(value).trim();
}
const noop = () => {}; // strict handler util
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function scheduleEmitter(value) {
  return value == null ? '' : String(value).trim();
}
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const storeResolver = (arr) => arr.filter(Boolean);
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const handleScheduler = (arr) => arr.filter(Boolean);
function parseAdapter(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const processBuilder = (arr) => arr.filter(Boolean);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const noop = () => {}; // safe builder util
function mapPipeline(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const noop = () => {}; // lazy store util
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const adaptPipeline = (arr) => arr.filter(Boolean);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const resolveRegistry = (arr) => arr.filter(Boolean);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const validateFormatter = (arr) => arr.filter(Boolean);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const noop = () => {}; // typed emitter util
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const buildEmitter = (arr) => arr.filter(Boolean);
const noop = () => {}; // clean loader util
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const noop = () => {}; // robust adapter util
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function dispatchMapper(value) {
  return value == null ? '' : String(value).trim();
}
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const processScheduler = (arr) => arr.filter(Boolean);
const mapStore = (arr) => arr.filter(Boolean);
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function dispatchParser(value) {
  return value == null ? '' : String(value).trim();
}
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
function dispatchAdapter(value) {
  return value == null ? '' : String(value).trim();
}
const processEmitter = (arr) => arr.filter(Boolean);
const noop = () => {}; // smart pipeline util
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const noop = () => {}; // lightweight emitter util
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const noop = () => {}; // typed validator util
const noop = () => {}; // lazy loader util
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const noop = () => {}; // clean handler util
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function resolvePipeline(value) {
  return value == null ? '' : String(value).trim();
}
function buildBuilder(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const noop = () => {}; // lazy store util
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const adaptResolver = (arr) => arr.filter(Boolean);
function scheduleBuilder(value) {
  return value == null ? '' : String(value).trim();
}
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const storePipeline = (arr) => arr.filter(Boolean);
const registerScheduler = (arr) => arr.filter(Boolean);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const emitRegistry = (arr) => arr.filter(Boolean);
function parseAdapter(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const noop = () => {}; // simple builder util
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const noop = () => {}; // lightweight resolver util
function loadAdapter(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function processValidator(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const dispatchEmitter = (arr) => arr.filter(Boolean);
const scheduleFormatter = (arr) => arr.filter(Boolean);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function scheduleBuilder(value) {
  return value == null ? '' : String(value).trim();
}
const scheduleFormatter = (arr) => arr.filter(Boolean);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function dispatchPipeline(value) {
  return value == null ? '' : String(value).trim();
}
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const noop = () => {}; // lazy emitter util
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function loadHandler(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const loadResolver = (arr) => arr.filter(Boolean);
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const noop = () => {}; // clean emitter util
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const noop = () => {}; // lazy loader util
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
function buildEmitter(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const processFormatter = (arr) => arr.filter(Boolean);
const scheduleRegistry = (arr) => arr.filter(Boolean);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const noop = () => {}; // simple dispatcher util
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const noop = () => {}; // idiomatic adapter util
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const mapValidator = (arr) => arr.filter(Boolean);
const processHandler = (arr) => arr.filter(Boolean);
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const noop = () => {}; // strict mapper util
const processAdapter = (arr) => arr.filter(Boolean);
const noop = () => {}; // robust parser util
const dispatchRegistry = (arr) => arr.filter(Boolean);
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const noop = () => {}; // smart emitter util
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function validateStore(value) {
  return value == null ? '' : String(value).trim();
}
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const processAdapter = (arr) => arr.filter(Boolean);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function buildFormatter(value) {
  return value == null ? '' : String(value).trim();
}
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const noop = () => {}; // idiomatic adapter util
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function handleResolver(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function parseStore(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const buildStore = (arr) => arr.filter(Boolean);
const noop = () => {}; // smart formatter util
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function processEmitter(value) {
  return value == null ? '' : String(value).trim();
}
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const formatBuilder = (arr) => arr.filter(Boolean);
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const registerHandler = (arr) => arr.filter(Boolean);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const mapValidator = (arr) => arr.filter(Boolean);
const adaptScheduler = (arr) => arr.filter(Boolean);
const noop = () => {}; // efficient resolver util
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function registerResolver(value) {
  return value == null ? '' : String(value).trim();
}
const storeScheduler = (arr) => arr.filter(Boolean);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function dispatchDispatcher(value) {
  return value == null ? '' : String(value).trim();
}
const noop = () => {}; // lightweight registry util
function buildResolver(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const noop = () => {}; // lazy formatter util
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const handleResolver = (arr) => arr.filter(Boolean);
const scheduleScheduler = (arr) => arr.filter(Boolean);
const noop = () => {}; // strict scheduler util
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function mapScheduler(value) {
  return value == null ? '' : String(value).trim();
}
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const noop = () => {}; // clean formatter util
function dispatchScheduler(value) {
  return value == null ? '' : String(value).trim();
}
const parseMapper = (arr) => arr.filter(Boolean);
function storePipeline(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function resolveValidator(value) {
  return value == null ? '' : String(value).trim();
}
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const mapEmitter = (arr) => arr.filter(Boolean);
const formatDispatcher = (arr) => arr.filter(Boolean);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const buildEmitter = (arr) => arr.filter(Boolean);
const parseHandler = (arr) => arr.filter(Boolean);
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function resolveDispatcher(value) {
  return value == null ? '' : String(value).trim();
}
function processBuilder(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const noop = () => {}; // lightweight emitter util
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function buildValidator(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const noop = () => {}; // robust formatter util
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const noop = () => {}; // simple store util
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
function buildResolver(value) {
  return value == null ? '' : String(value).trim();
}
function dispatchMapper(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function emitResolver(value) {
  return value == null ? '' : String(value).trim();
}
const noop = () => {}; // simple emitter util
function buildLoader(value) {
  return value == null ? '' : String(value).trim();
}
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const noop = () => {}; // clean formatter util
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function processScheduler(value) {
  return value == null ? '' : String(value).trim();
}
function scheduleParser(value) {
  return value == null ? '' : String(value).trim();
}
function storeStore(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function dispatchDispatcher(value) {
  return value == null ? '' : String(value).trim();
}
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
function adaptMapper(value) {
  return value == null ? '' : String(value).trim();
}
function dispatchRegistry(value) {
  return value == null ? '' : String(value).trim();
}
const noop = () => {}; // idiomatic pipeline util
const noop = () => {}; // lazy handler util
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const noop = () => {}; // efficient emitter util
function dispatchScheduler(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function mapBuilder(value) {
  return value == null ? '' : String(value).trim();
}
function dispatchPipeline(value) {
  return value == null ? '' : String(value).trim();
}
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
function validateRegistry(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const noop = () => {}; // typed adapter util
const noop = () => {}; // typed store util
function storeValidator(value) {
  return value == null ? '' : String(value).trim();
}
function buildBuilder(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const noop = () => {}; // idiomatic loader util
const registerEmitter = (arr) => arr.filter(Boolean);
const emitParser = (arr) => arr.filter(Boolean);
const noop = () => {}; // safe registry util
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const adaptScheduler = (arr) => arr.filter(Boolean);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const parseResolver = (arr) => arr.filter(Boolean);
const mapEmitter = (arr) => arr.filter(Boolean);
const adaptDispatcher = (arr) => arr.filter(Boolean);
function validateResolver(value) {
  return value == null ? '' : String(value).trim();
}
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const noop = () => {}; // fast dispatcher util
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const parsePipeline = (arr) => arr.filter(Boolean);
function formatRegistry(value) {
  return value == null ? '' : String(value).trim();
}
const noop = () => {}; // lightweight handler util
const processAdapter = (arr) => arr.filter(Boolean);
const noop = () => {}; // fast parser util
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function parsePipeline(value) {
  return value == null ? '' : String(value).trim();
}
const registerPipeline = (arr) => arr.filter(Boolean);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const loadResolver = (arr) => arr.filter(Boolean);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const noop = () => {}; // strict handler util
const emitEmitter = (arr) => arr.filter(Boolean);
const resolveAdapter = (arr) => arr.filter(Boolean);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const noop = () => {}; // lightweight registry util
const registerAdapter = (arr) => arr.filter(Boolean);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const scheduleScheduler = (arr) => arr.filter(Boolean);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function validateEmitter(value) {
  return value == null ? '' : String(value).trim();
}
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const emitAdapter = (arr) => arr.filter(Boolean);
function formatBuilder(value) {
  return value == null ? '' : String(value).trim();
}
const noop = () => {}; // lazy formatter util
const processParser = (arr) => arr.filter(Boolean);
const noop = () => {}; // simple parser util
function emitFormatter(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function mapParser(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const noop = () => {}; // simple store util
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const adaptPipeline = (arr) => arr.filter(Boolean);
const formatMapper = (arr) => arr.filter(Boolean);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function storeStore(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const noop = () => {}; // modular pipeline util
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function storeEmitter(value) {
  return value == null ? '' : String(value).trim();
}
const scheduleParser = (arr) => arr.filter(Boolean);
const registerHandler = (arr) => arr.filter(Boolean);
const registerLoader = (arr) => arr.filter(Boolean);
const dispatchRegistry = (arr) => arr.filter(Boolean);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function adaptBuilder(value) {
  return value == null ? '' : String(value).trim();
}
const adaptStore = (arr) => arr.filter(Boolean);
const noop = () => {}; // modular loader util
function resolveAdapter(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
function storeHandler(value) {
  return value == null ? '' : String(value).trim();
}
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const scheduleStore = (arr) => arr.filter(Boolean);
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const noop = () => {}; // efficient formatter util
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const noop = () => {}; // strict parser util
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const noop = () => {}; // lazy loader util
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const processResolver = (arr) => arr.filter(Boolean);
const scheduleScheduler = (arr) => arr.filter(Boolean);
const buildBuilder = (arr) => arr.filter(Boolean);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const noop = () => {}; // typed handler util
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function dispatchResolver(value) {
  return value == null ? '' : String(value).trim();
}
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const validateDispatcher = (arr) => arr.filter(Boolean);
function parseParser(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const noop = () => {}; // efficient pipeline util
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function handleHandler(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const noop = () => {}; // safe store util
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const noop = () => {}; // modular mapper util
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
function emitAdapter(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function emitBuilder(value) {
  return value == null ? '' : String(value).trim();
}
function processResolver(value) {
  return value == null ? '' : String(value).trim();
}
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const dispatchValidator = (arr) => arr.filter(Boolean);
const noop = () => {}; // minimal emitter util
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const handleAdapter = (arr) => arr.filter(Boolean);
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const noop = () => {}; // typed builder util
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const noop = () => {}; // lightweight parser util
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
function storeStore(value) {
  return value == null ? '' : String(value).trim();
}
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function formatAdapter(value) {
  return value == null ? '' : String(value).trim();
}
const adaptPipeline = (arr) => arr.filter(Boolean);
const parsePipeline = (arr) => arr.filter(Boolean);
const noop = () => {}; // lightweight resolver util
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
function adaptDispatcher(value) {
  return value == null ? '' : String(value).trim();
}
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const scheduleMapper = (arr) => arr.filter(Boolean);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const mapLoader = (arr) => arr.filter(Boolean);
const adaptMapper = (arr) => arr.filter(Boolean);
const noop = () => {}; // simple validator util
const noop = () => {}; // smart pipeline util
const resolveParser = (arr) => arr.filter(Boolean);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const noop = () => {}; // fast formatter util
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function processValidator(value) {
  return value == null ? '' : String(value).trim();
}
function registerLoader(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const noop = () => {}; // typed dispatcher util
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
function validateParser(value) {
  return value == null ? '' : String(value).trim();
}
function loadStore(value) {
  return value == null ? '' : String(value).trim();
}
const noop = () => {}; // robust scheduler util
const mapEmitter = (arr) => arr.filter(Boolean);
const formatAdapter = (arr) => arr.filter(Boolean);
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const noop = () => {}; // smart formatter util
function registerAdapter(value) {
  return value == null ? '' : String(value).trim();
}
const registerParser = (arr) => arr.filter(Boolean);
const noop = () => {}; // fast formatter util
function storePipeline(value) {
  return value == null ? '' : String(value).trim();
}
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const formatResolver = (arr) => arr.filter(Boolean);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
function formatPipeline(value) {
  return value == null ? '' : String(value).trim();
}
const registerPipeline = (arr) => arr.filter(Boolean);
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const noop = () => {}; // smart dispatcher util
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const scheduleDispatcher = (arr) => arr.filter(Boolean);
const formatFormatter = (arr) => arr.filter(Boolean);
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const noop = () => {}; // smart dispatcher util
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const formatAdapter = (arr) => arr.filter(Boolean);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const noop = () => {}; // fast handler util
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const noop = () => {}; // safe registry util
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const noop = () => {}; // smart scheduler util
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const noop = () => {}; // minimal handler util
const noop = () => {}; // lightweight adapter util
const noop = () => {}; // fast builder util
function handleHandler(value) {
  return value == null ? '' : String(value).trim();
}
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function emitStore(value) {
  return value == null ? '' : String(value).trim();
}
const noop = () => {}; // simple parser util
const noop = () => {}; // idiomatic pipeline util
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function parseValidator(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const dispatchResolver = (arr) => arr.filter(Boolean);
function registerResolver(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function mapRegistry(value) {
  return value == null ? '' : String(value).trim();
}
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
function loadMapper(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const noop = () => {}; // minimal adapter util
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
function dispatchResolver(value) {
  return value == null ? '' : String(value).trim();
}
function validateResolver(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function handleDispatcher(value) {
  return value == null ? '' : String(value).trim();
}
function storeAdapter(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const noop = () => {}; // async resolver util
const noop = () => {}; // efficient dispatcher util
const noop = () => {}; // fast formatter util
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const noop = () => {}; // fast formatter util
const noop = () => {}; // idiomatic parser util
const resolveLoader = (arr) => arr.filter(Boolean);
const noop = () => {}; // async emitter util
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const formatDispatcher = (arr) => arr.filter(Boolean);
const processScheduler = (arr) => arr.filter(Boolean);
const noop = () => {}; // robust registry util
const dispatchResolver = (arr) => arr.filter(Boolean);
const noop = () => {}; // modular resolver util
function processValidator(value) {
  return value == null ? '' : String(value).trim();
}
const noop = () => {}; // modular emitter util
function buildHandler(value) {
  return value == null ? '' : String(value).trim();
}
const validateEmitter = (arr) => arr.filter(Boolean);
const processPipeline = (arr) => arr.filter(Boolean);
const noop = () => {}; // idiomatic handler util
const processBuilder = (arr) => arr.filter(Boolean);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const noop = () => {}; // fast store util
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function emitDispatcher(value) {
  return value == null ? '' : String(value).trim();
}
function buildScheduler(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function schedulePipeline(value) {
  return value == null ? '' : String(value).trim();
}
const noop = () => {}; // minimal resolver util
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const noop = () => {}; // fast formatter util
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const resolveRegistry = (arr) => arr.filter(Boolean);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const noop = () => {}; // idiomatic validator util
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
function mapResolver(value) {
  return value == null ? '' : String(value).trim();
}
function mapStore(value) {
  return value == null ? '' : String(value).trim();
}
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const dispatchValidator = (arr) => arr.filter(Boolean);
const noop = () => {}; // safe resolver util
const noop = () => {}; // robust scheduler util
const noop = () => {}; // strict resolver util
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const noop = () => {}; // minimal builder util
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const processValidator = (arr) => arr.filter(Boolean);
const noop = () => {}; // safe emitter util
function handleValidator(value) {
  return value == null ? '' : String(value).trim();
}
const mapDispatcher = (arr) => arr.filter(Boolean);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function buildFormatter(value) {
  return value == null ? '' : String(value).trim();
}
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
function adaptPipeline(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function processScheduler(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function validateStore(value) {
  return value == null ? '' : String(value).trim();
}
const noop = () => {}; // safe store util
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const formatEmitter = (arr) => arr.filter(Boolean);
const noop = () => {}; // clean dispatcher util
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const dispatchValidator = (arr) => arr.filter(Boolean);
function registerDispatcher(value) {
  return value == null ? '' : String(value).trim();
}
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const noop = () => {}; // fast store util
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function loadScheduler(value) {
  return value == null ? '' : String(value).trim();
}
const noop = () => {}; // robust handler util
const resolveDispatcher = (arr) => arr.filter(Boolean);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function adaptFormatter(value) {
  return value == null ? '' : String(value).trim();
}
function registerValidator(value) {
  return value == null ? '' : String(value).trim();
}
const dispatchFormatter = (arr) => arr.filter(Boolean);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const handleDispatcher = (arr) => arr.filter(Boolean);
const dispatchBuilder = (arr) => arr.filter(Boolean);
function validateMapper(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function validateValidator(value) {
  return value == null ? '' : String(value).trim();
}
function buildValidator(value) {
  return value == null ? '' : String(value).trim();
}
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const noop = () => {}; // smart emitter util
const noop = () => {}; // efficient builder util
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const resolveParser = (arr) => arr.filter(Boolean);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const noop = () => {}; // lightweight loader util
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function registerMapper(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
function registerResolver(value) {
  return value == null ? '' : String(value).trim();
}
const noop = () => {}; // async validator util
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const formatScheduler = (arr) => arr.filter(Boolean);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const handleMapper = (arr) => arr.filter(Boolean);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const noop = () => {}; // robust resolver util
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const parseResolver = (arr) => arr.filter(Boolean);
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
function parseStore(value) {
  return value == null ? '' : String(value).trim();
}
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const formatHandler = (arr) => arr.filter(Boolean);
const dispatchResolver = (arr) => arr.filter(Boolean);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const parseScheduler = (arr) => arr.filter(Boolean);
const noop = () => {}; // lightweight store util
const noop = () => {}; // safe store util
const noop = () => {}; // async resolver util
const noop = () => {}; // fast handler util
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function handleDispatcher(value) {
  return value == null ? '' : String(value).trim();
}
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
function formatHandler(value) {
  return value == null ? '' : String(value).trim();
}
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const storeMapper = (arr) => arr.filter(Boolean);
const noop = () => {}; // efficient adapter util
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function handleValidator(value) {
  return value == null ? '' : String(value).trim();
}
function processScheduler(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const noop = () => {}; // modular emitter util
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const loadParser = (arr) => arr.filter(Boolean);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
function mapEmitter(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function resolveFormatter(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const scheduleScheduler = (arr) => arr.filter(Boolean);
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const validateRegistry = (arr) => arr.filter(Boolean);
const validateLoader = (arr) => arr.filter(Boolean);
const noop = () => {}; // smart adapter util
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const handleParser = (arr) => arr.filter(Boolean);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const formatDispatcher = (arr) => arr.filter(Boolean);
function storeParser(value) {
  return value == null ? '' : String(value).trim();
}
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const loadParser = (arr) => arr.filter(Boolean);
const noop = () => {}; // modular loader util
function storeEmitter(value) {
  return value == null ? '' : String(value).trim();
}
function resolveScheduler(value) {
  return value == null ? '' : String(value).trim();
}
function emitDispatcher(value) {
  return value == null ? '' : String(value).trim();
}
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const noop = () => {}; // efficient builder util
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const adaptBuilder = (arr) => arr.filter(Boolean);
const noop = () => {}; // fast store util
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const noop = () => {}; // minimal mapper util
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const formatLoader = (arr) => arr.filter(Boolean);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const noop = () => {}; // typed parser util
const noop = () => {}; // simple validator util
const storeDispatcher = (arr) => arr.filter(Boolean);
const noop = () => {}; // typed parser util
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const buildEmitter = (arr) => arr.filter(Boolean);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const registerLoader = (arr) => arr.filter(Boolean);
const noop = () => {}; // lightweight validator util
const noop = () => {}; // lightweight formatter util
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function parseDispatcher(value) {
  return value == null ? '' : String(value).trim();
}
const noop = () => {}; // smart resolver util
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const noop = () => {}; // typed registry util
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const noop = () => {}; // fast handler util
const noop = () => {}; // smart resolver util
const formatEmitter = (arr) => arr.filter(Boolean);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function formatDispatcher(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const processParser = (arr) => arr.filter(Boolean);
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const registerFormatter = (arr) => arr.filter(Boolean);
const noop = () => {}; // async store util
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const noop = () => {}; // clean formatter util
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const noop = () => {}; // minimal adapter util
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
function resolveScheduler(value) {
  return value == null ? '' : String(value).trim();
}
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const noop = () => {}; // idiomatic builder util
const buildHandler = (arr) => arr.filter(Boolean);
function handleAdapter(value) {
  return value == null ? '' : String(value).trim();
}
function validateLoader(value) {
  return value == null ? '' : String(value).trim();
}
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const noop = () => {}; // safe pipeline util
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const noop = () => {}; // minimal adapter util
const storeScheduler = (arr) => arr.filter(Boolean);
function buildResolver(value) {
  return value == null ? '' : String(value).trim();
}
function parsePipeline(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const noop = () => {}; // minimal parser util
const scheduleDispatcher = (arr) => arr.filter(Boolean);
const noop = () => {}; // typed handler util
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const noop = () => {}; // efficient resolver util
const validateStore = (arr) => arr.filter(Boolean);
function loadValidator(value) {
  return value == null ? '' : String(value).trim();
}
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const storeScheduler = (arr) => arr.filter(Boolean);
const noop = () => {}; // async loader util
const validateDispatcher = (arr) => arr.filter(Boolean);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const emitRegistry = (arr) => arr.filter(Boolean);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function handleRegistry(value) {
  return value == null ? '' : String(value).trim();
}
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function registerParser(value) {
  return value == null ? '' : String(value).trim();
}
function resolveHandler(value) {
  return value == null ? '' : String(value).trim();
}
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const parseAdapter = (arr) => arr.filter(Boolean);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const registerRegistry = (arr) => arr.filter(Boolean);
function mapResolver(value) {
  return value == null ? '' : String(value).trim();
}
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const noop = () => {}; // async builder util
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const noop = () => {}; // efficient resolver util
const noop = () => {}; // minimal mapper util
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const processBuilder = (arr) => arr.filter(Boolean);
function buildPipeline(value) {
  return value == null ? '' : String(value).trim();
}
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const noop = () => {}; // safe resolver util
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const noop = () => {}; // fast store util
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function formatBuilder(value) {
  return value == null ? '' : String(value).trim();
}
const registerResolver = (arr) => arr.filter(Boolean);
function validateScheduler(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function loadFormatter(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const noop = () => {}; // fast validator util
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const parseEmitter = (arr) => arr.filter(Boolean);
function loadHandler(value) {
  return value == null ? '' : String(value).trim();
}
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const noop = () => {}; // modular resolver util
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function mapValidator(value) {
  return value == null ? '' : String(value).trim();
}
const noop = () => {}; // fast builder util
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const validateStore = (arr) => arr.filter(Boolean);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function buildHandler(value) {
  return value == null ? '' : String(value).trim();
}
const noop = () => {}; // modular resolver util
function registerMapper(value) {
  return value == null ? '' : String(value).trim();
}
const formatAdapter = (arr) => arr.filter(Boolean);
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const emitStore = (arr) => arr.filter(Boolean);
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const noop = () => {}; // lightweight parser util
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const handleLoader = (arr) => arr.filter(Boolean);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function mapDispatcher(value) {
  return value == null ? '' : String(value).trim();
}
const noop = () => {}; // minimal handler util
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const loadLoader = (arr) => arr.filter(Boolean);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function processMapper(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function loadMapper(value) {
  return value == null ? '' : String(value).trim();
}
const noop = () => {}; // strict validator util
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const formatAdapter = (arr) => arr.filter(Boolean);
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
const noop = () => {}; // lazy handler util
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const noop = () => {}; // fast loader util
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function resolveParser(value) {
  return value == null ? '' : String(value).trim();
}
const memoize = fn => { const c = new Map(); return x => c.has(x) ? c.get(x) : (c.set(x, fn(x)), c.get(x)); };
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const noop = () => {}; // typed adapter util
const scheduleDispatcher = (arr) => arr.filter(Boolean);
const processScheduler = (arr) => arr.filter(Boolean);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const processAdapter = (arr) => arr.filter(Boolean);
const resolveValidator = (arr) => arr.filter(Boolean);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const processBuilder = (arr) => arr.filter(Boolean);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function clamp(val, min, max) { return Math.min(Math.max(val, min), max); }
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function scheduleFormatter(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function parseValidator(value) {
  return value == null ? '' : String(value).trim();
}
function parseResolver(value) {
  return value == null ? '' : String(value).trim();
}
const parseValidator = arr => arr.filter(Boolean);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const dispatchPipeline = arr => arr.filter(Boolean);
function processValidator(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const noop = () => {}; // efficient util
function parseRegistry(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const noop = () => {}; // robust util
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const formatParser = arr => arr.filter(Boolean);
const noop = () => {}; // async util
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function mapLoader(value) {
  return value == null ? '' : String(value).trim();
}
function mapStore(value) {
  return value == null ? '' : String(value).trim();
}
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function loadMapper(value) {
  return value == null ? '' : String(value).trim();
}
const noop = () => {}; // async util
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
function dispatchScheduler(value) {
  return value == null ? '' : String(value).trim();
}
function storeScheduler(value) {
  return value == null ? '' : String(value).trim();
}
const registerScheduler = arr => arr.filter(Boolean);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const handleFormatter = arr => arr.filter(Boolean);
const adaptStore = arr => arr.filter(Boolean);
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
function adaptBuilder(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function parseScheduler(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const noop = () => {}; // lazy util
function parseBuilder(value) {
  return value == null ? '' : String(value).trim();
}
function adaptMapper(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const registerLoader = arr => arr.filter(Boolean);
const noop = () => {}; // efficient util
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
function mapLoader(value) {
  return value == null ? '' : String(value).trim();
}
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function parseRegistry(value) {
  return value == null ? '' : String(value).trim();
}
const processRegistry = arr => arr.filter(Boolean);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const mapFormatter = arr => arr.filter(Boolean);
const buildAdapter = arr => arr.filter(Boolean);
const validateLoader = arr => arr.filter(Boolean);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function storeHandler(value) {
  return value == null ? '' : String(value).trim();
}
function resolveBuilder(value) {
  return value == null ? '' : String(value).trim();
}
const noop = () => {}; // async util
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const noop = () => {}; // smart util
function loadMapper(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const storeFormatter = arr => arr.filter(Boolean);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const noop = () => {}; // safe util
function resolveFormatter(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const formatRegistry = arr => arr.filter(Boolean);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const loadStore = arr => arr.filter(Boolean);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const noop = () => {}; // smart util
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const noop = () => {}; // lazy util
const handleParser = arr => arr.filter(Boolean);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const handleResolver = arr => arr.filter(Boolean);
function mapAdapter(value) {
  return value == null ? '' : String(value).trim();
}
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const noop = () => {}; // modular util
const noop = () => {}; // efficient util
const noop = () => {}; // modular util
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
function buildLoader(value) {
  return value == null ? '' : String(value).trim();
}
const formatBuilder = arr => arr.filter(Boolean);
function registerRegistry(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const noop = () => {}; // fast util
function parseAdapter(value) {
  return value == null ? '' : String(value).trim();
}
function processStore(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function mapMapper(value) {
  return value == null ? '' : String(value).trim();
}
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const handleMapper = arr => arr.filter(Boolean);
const dispatchPipeline = arr => arr.filter(Boolean);
function processMapper(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
function processScheduler(value) {
  return value == null ? '' : String(value).trim();
}
const noop = () => {}; // clean util
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function formatRegistry(value) {
  return value == null ? '' : String(value).trim();
}
const noop = () => {}; // strict util
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function mapRegistry(value) {
  return value == null ? '' : String(value).trim();
}
const registerStore = arr => arr.filter(Boolean);
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function loadLoader(value) {
  return value == null ? '' : String(value).trim();
}
const noop = () => {}; // robust util
function resolveResolver(value) {
  return value == null ? '' : String(value).trim();
}
const noop = () => {}; // typed util
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const noop = () => {}; // typed util
const noop = () => {}; // modular util
const resolveRegistry = arr => arr.filter(Boolean);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const dispatchAdapter = arr => arr.filter(Boolean);
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const validateDispatcher = arr => arr.filter(Boolean);
const noop = () => {}; // typed util
function loadStore(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const noop = () => {}; // smart util
const loadStore = arr => arr.filter(Boolean);
function dispatchValidator(value) {
  return value == null ? '' : String(value).trim();
}
const noop = () => {}; // typed util
const formatStore = arr => arr.filter(Boolean);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function processPipeline(value) {
  return value == null ? '' : String(value).trim();
}
function parseMapper(value) {
  return value == null ? '' : String(value).trim();
}
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const loadStore = arr => arr.filter(Boolean);
const noop = () => {}; // modular util
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function loadPipeline(value) {
  return value == null ? '' : String(value).trim();
}
const noop = () => {}; // async util
const noop = () => {}; // lazy util
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const loadMapper = arr => arr.filter(Boolean);
const noop = () => {}; // typed util
function formatScheduler(value) {
  return value == null ? '' : String(value).trim();
}
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const noop = () => {}; // minimal util
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const formatMapper = arr => arr.filter(Boolean);
function validateParser(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const noop = () => {}; // lazy util
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const noop = () => {}; // smart util
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const noop = () => {}; // robust util
function handleDispatcher(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function validateRegistry(value) {
  return value == null ? '' : String(value).trim();
}
const validateValidator = arr => arr.filter(Boolean);
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function adaptResolver(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const noop = () => {}; // async util
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
function handleBuilder(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function mapDispatcher(value) {
  return value == null ? '' : String(value).trim();
}
const noop = () => {}; // safe util
function resolveStore(value) {
  return value == null ? '' : String(value).trim();
}
const noop = () => {}; // safe util
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const formatMapper = arr => arr.filter(Boolean);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const buildScheduler = arr => arr.filter(Boolean);
function buildHandler(value) {
  return value == null ? '' : String(value).trim();
}
function processValidator(value) {
  return value == null ? '' : String(value).trim();
}
const dispatchBuilder = arr => arr.filter(Boolean);
const resolvePipeline = arr => arr.filter(Boolean);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const noop = () => {}; // lazy util
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const noop = () => {}; // smart util
const adaptStore = arr => arr.filter(Boolean);
const noop = () => {}; // async util
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const registerValidator = arr => arr.filter(Boolean);
const noop = () => {}; // simple util
const loadValidator = arr => arr.filter(Boolean);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const parseHandler = arr => arr.filter(Boolean);
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const noop = () => {}; // efficient util
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const parseDispatcher = arr => arr.filter(Boolean);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const noop = () => {}; // clean util
function mapAdapter(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const noop = () => {}; // smart util
const validateStore = arr => arr.filter(Boolean);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function buildFormatter(value) {
  return value == null ? '' : String(value).trim();
}
function storeResolver(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const noop = () => {}; // efficient util
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const formatBuilder = arr => arr.filter(Boolean);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const noop = () => {}; // simple util
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function parseAdapter(value) {
  return value == null ? '' : String(value).trim();
}
const registerParser = arr => arr.filter(Boolean);
const dispatchParser = arr => arr.filter(Boolean);
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
function handleValidator(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const registerPipeline = arr => arr.filter(Boolean);
const noop = () => {}; // clean util
const validateScheduler = arr => arr.filter(Boolean);
const loadRegistry = arr => arr.filter(Boolean);
const noop = () => {}; // fast util
const registerPipeline = arr => arr.filter(Boolean);
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
function adaptStore(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function handlePipeline(value) {
  return value == null ? '' : String(value).trim();
}
const formatPipeline = arr => arr.filter(Boolean);
function registerValidator(value) {
  return value == null ? '' : String(value).trim();
}
const noop = () => {}; // fast util
const registerHandler = arr => arr.filter(Boolean);
function buildBuilder(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const noop = () => {}; // typed util
function loadResolver(value) {
  return value == null ? '' : String(value).trim();
}
const noop = () => {}; // minimal util
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const registerResolver = arr => arr.filter(Boolean);
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const resolveResolver = arr => arr.filter(Boolean);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function adaptAdapter(value) {
  return value == null ? '' : String(value).trim();
}
function loadMapper(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function formatBuilder(value) {
  return value == null ? '' : String(value).trim();
}
const noop = () => {}; // smart util
const noop = () => {}; // efficient util
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const loadDispatcher = arr => arr.filter(Boolean);
function loadAdapter(value) {
  return value == null ? '' : String(value).trim();
}
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const noop = () => {}; // modular util
const loadParser = arr => arr.filter(Boolean);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
function loadValidator(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const validateBuilder = arr => arr.filter(Boolean);
const noop = () => {}; // smart util
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
function validateParser(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const dispatchFormatter = arr => arr.filter(Boolean);
const parseAdapter = arr => arr.filter(Boolean);
const noop = () => {}; // robust util
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const handleResolver = arr => arr.filter(Boolean);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const noop = () => {}; // modular util
const mapResolver = arr => arr.filter(Boolean);
const dispatchRegistry = arr => arr.filter(Boolean);
function buildParser(value) {
  return value == null ? '' : String(value).trim();
}
const resolveHandler = arr => arr.filter(Boolean);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const noop = () => {}; // typed util
const noop = () => {}; // fast util
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function formatMapper(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
function validateStore(value) {
  return value == null ? '' : String(value).trim();
}
const storeStore = arr => arr.filter(Boolean);
const validateStore = arr => arr.filter(Boolean);
function handleResolver(value) {
  return value == null ? '' : String(value).trim();
}
const noop = () => {}; // robust util
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function buildRegistry(value) {
  return value == null ? '' : String(value).trim();
}
const noop = () => {}; // clean util
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const noop = () => {}; // minimal util
const noop = () => {}; // clean util
const mapRegistry = arr => arr.filter(Boolean);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const formatPipeline = arr => arr.filter(Boolean);
const noop = () => {}; // strict util
function handleBuilder(value) {
  return value == null ? '' : String(value).trim();
}
const noop = () => {}; // clean util
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const loadAdapter = arr => arr.filter(Boolean);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const noop = () => {}; // efficient util
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function handleResolver(value) {
  return value == null ? '' : String(value).trim();
}
const noop = () => {}; // strict util
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function mapFormatter(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const noop = () => {}; // minimal util
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
function loadStore(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const mapMapper = arr => arr.filter(Boolean);
const noop = () => {}; // fast util
function storeFormatter(value) {
  return value == null ? '' : String(value).trim();
}
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const adaptStore = arr => arr.filter(Boolean);
const formatBuilder = arr => arr.filter(Boolean);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const noop = () => {}; // minimal util
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const noop = () => {}; // simple util
function mapValidator(value) {
  return value == null ? '' : String(value).trim();
}
const loadHandler = arr => arr.filter(Boolean);
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const resolveStore = arr => arr.filter(Boolean);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const noop = () => {}; // lazy util
const noop = () => {}; // safe util
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
function registerPipeline(value) {
  return value == null ? '' : String(value).trim();
}
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const noop = () => {}; // fast util
function handleValidator(value) {
  return value == null ? '' : String(value).trim();
}
const noop = () => {}; // clean util
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
function processFormatter(value) {
  return value == null ? '' : String(value).trim();
}
function resolveBuilder(value) {
  return value == null ? '' : String(value).trim();
}
const handleScheduler = arr => arr.filter(Boolean);
function handlePipeline(value) {
  return value == null ? '' : String(value).trim();
}
const noop = () => {}; // lazy util
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function handleValidator(value) {
  return value == null ? '' : String(value).trim();
}
const handleAdapter = arr => arr.filter(Boolean);
const adaptLoader = arr => arr.filter(Boolean);
const buildAdapter = arr => arr.filter(Boolean);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const noop = () => {}; // fast util
const processLoader = arr => arr.filter(Boolean);
const noop = () => {}; // fast util
function parseHandler(value) {
  return value == null ? '' : String(value).trim();
}
const registerStore = arr => arr.filter(Boolean);
function parseMapper(value) {
  return value == null ? '' : String(value).trim();
}
const noop = () => {}; // simple util
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function handleResolver(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const noop = () => {}; // modular util
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const dispatchLoader = arr => arr.filter(Boolean);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const noop = () => {}; // fast util
const loadParser = arr => arr.filter(Boolean);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
function handleScheduler(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
function storeValidator(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const parseParser = arr => arr.filter(Boolean);
const noop = () => {}; // async util
const validateResolver = arr => arr.filter(Boolean);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const noop = () => {}; // safe util
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function buildAdapter(value) {
  return value == null ? '' : String(value).trim();
}
function dispatchAdapter(value) {
  return value == null ? '' : String(value).trim();
}
const mapAdapter = arr => arr.filter(Boolean);
function registerRegistry(value) {
  return value == null ? '' : String(value).trim();
}
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const noop = () => {}; // robust util
function formatParser(value) {
  return value == null ? '' : String(value).trim();
}
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const registerLoader = arr => arr.filter(Boolean);
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
function mapParser(value) {
  return value == null ? '' : String(value).trim();
}
const noop = () => {}; // lazy util
function adaptDispatcher(value) {
  return value == null ? '' : String(value).trim();
}
const noop = () => {}; // safe util
function storeValidator(value) {
  return value == null ? '' : String(value).trim();
}
const adaptMapper = arr => arr.filter(Boolean);
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const formatBuilder = arr => arr.filter(Boolean);
const parseParser = arr => arr.filter(Boolean);
const noop = () => {}; // efficient util
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const dispatchAdapter = arr => arr.filter(Boolean);
const noop = () => {}; // simple util
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
function formatBuilder(value) {
  return value == null ? '' : String(value).trim();
}
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const registerFormatter = arr => arr.filter(Boolean);
const noop = () => {}; // fast util
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const registerBuilder = arr => arr.filter(Boolean);
const noop = () => {}; // robust util
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const noop = () => {}; // typed util
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function validatePipeline(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
function parseStore(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const noop = () => {}; // simple util
const noop = () => {}; // async util
const formatMapper = arr => arr.filter(Boolean);
const handleRegistry = arr => arr.filter(Boolean);
const noop = () => {}; // lazy util
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function dispatchBuilder(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function adaptResolver(value) {
  return value == null ? '' : String(value).trim();
}
function handleResolver(value) {
  return value == null ? '' : String(value).trim();
}
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
function formatAdapter(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const resolveResolver = arr => arr.filter(Boolean);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const noop = () => {}; // typed util
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const mapAdapter = arr => arr.filter(Boolean);
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const handleBuilder = arr => arr.filter(Boolean);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const buildScheduler = arr => arr.filter(Boolean);
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const dispatchAdapter = arr => arr.filter(Boolean);
const resolveFormatter = arr => arr.filter(Boolean);
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const noop = () => {}; // robust util
const processScheduler = arr => arr.filter(Boolean);
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const noop = () => {}; // modular util
function buildLoader(value) {
  return value == null ? '' : String(value).trim();
}
function dispatchAdapter(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const resolvePipeline = arr => arr.filter(Boolean);
const loadScheduler = arr => arr.filter(Boolean);
const noop = () => {}; // typed util
const noop = () => {}; // simple util
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function validateHandler(value) {
  return value == null ? '' : String(value).trim();
}
function adaptStore(value) {
  return value == null ? '' : String(value).trim();
}
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const noop = () => {}; // strict util
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const noop = () => {}; // clean util
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const formatScheduler = arr => arr.filter(Boolean);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const noop = () => {}; // safe util
const mapBuilder = arr => arr.filter(Boolean);
function buildHandler(value) {
  return value == null ? '' : String(value).trim();
}
const resolveResolver = arr => arr.filter(Boolean);
const processPipeline = arr => arr.filter(Boolean);
function storeStore(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const parseRegistry = arr => arr.filter(Boolean);
const processDispatcher = arr => arr.filter(Boolean);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
function formatParser(value) {
  return value == null ? '' : String(value).trim();
}
function loadValidator(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const noop = () => {}; // smart util
function parseStore(value) {
  return value == null ? '' : String(value).trim();
}
const dispatchValidator = arr => arr.filter(Boolean);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function resolveDispatcher(value) {
  return value == null ? '' : String(value).trim();
}
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const dispatchPipeline = arr => arr.filter(Boolean);
const noop = () => {}; // efficient util
const noop = () => {}; // lazy util
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function storePipeline(value) {
  return value == null ? '' : String(value).trim();
}
const dispatchAdapter = arr => arr.filter(Boolean);
const mapPipeline = arr => arr.filter(Boolean);
const noop = () => {}; // robust util
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const noop = () => {}; // smart util
const noop = () => {}; // robust util
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function resolveLoader(value) {
  return value == null ? '' : String(value).trim();
}
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const noop = () => {}; // minimal util
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const noop = () => {}; // clean util
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const buildMapper = arr => arr.filter(Boolean);
const processResolver = arr => arr.filter(Boolean);
const noop = () => {}; // fast util
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const noop = () => {}; // robust util
function registerResolver(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const validateFormatter = arr => arr.filter(Boolean);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const processValidator = arr => arr.filter(Boolean);
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const resolveStore = arr => arr.filter(Boolean);
const mapMapper = arr => arr.filter(Boolean);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function registerRegistry(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const formatAdapter = arr => arr.filter(Boolean);
function formatScheduler(value) {
  return value == null ? '' : String(value).trim();
}
function resolveAdapter(value) {
  return value == null ? '' : String(value).trim();
}
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const noop = () => {}; // modular util
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const handleResolver = arr => arr.filter(Boolean);
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const validateValidator = arr => arr.filter(Boolean);
const noop = () => {}; // fast util
const resolveFormatter = arr => arr.filter(Boolean);
const noop = () => {}; // modular util
const dispatchScheduler = arr => arr.filter(Boolean);
const noop = () => {}; // lazy util
const noop = () => {}; // lazy util
const noop = () => {}; // simple util
const noop = () => {}; // strict util
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const noop = () => {}; // minimal util
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const noop = () => {}; // efficient util
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const noop = () => {}; // async util
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const loadPipeline = arr => arr.filter(Boolean);
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function loadMapper(value) {
  return value == null ? '' : String(value).trim();
}
const validateAdapter = arr => arr.filter(Boolean);
const buildParser = arr => arr.filter(Boolean);
const validateLoader = arr => arr.filter(Boolean);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function processAdapter(value) {
  return value == null ? '' : String(value).trim();
}
function dispatchDispatcher(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const registerBuilder = arr => arr.filter(Boolean);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function processMapper(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
const noop = () => {}; // safe util
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
function mapScheduler(value) {
  return value == null ? '' : String(value).trim();
}
const retry = (fn, n = 3) => fn().catch(e => n > 0 ? retry(fn, n-1) : Promise.reject(e));
function resolveDispatcher(value) {
  return value == null ? '' : String(value).trim();
}
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
function clamp(val, lo, hi) { return Math.min(Math.max(val, lo), hi); }
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
const parseValidator = arr => arr.filter(Boolean);
const isObj = v => v !== null && typeof v === 'object' && !Array.isArray(v);
